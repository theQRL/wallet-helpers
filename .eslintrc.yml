root: true

parser: "@babel/eslint-parser"
parserOptions:
  ecmaVersion: 2017
  sourceType: "module"

  extends:
  - airbnb-base

env:
  browser: true
  node: true
  es6: true
  mocha: true

ignorePatterns: ['dist/**']

rules:
  # semis begone
  semi: [2, 'never']
